# 크레이지 아케이드 클론 — 설계 문서

> 상태: v1.0 — M1~M8 구현 완료 / 최종 수정 2026-08-05
>
> 이 문서는 코드를 따라간다. 결정의 *이유*와 관측된 *수치*를 남기는 것이 목적이고,
> 규칙과 상태 모델은 `core`의 실제 타입과 맞춰 둔다.

## 0. 한 줄 요약

격자 기반 4인 대전 액션. 물풍선을 놓아 십자 물줄기로 상대를 **물방울에 가두고**,
갇힌 상대를 밟아 제거한다. 1단계 목표였던 **싱글 플레이어 + AI 봇**과
최종 목표였던 **온라인 대전**이 모두 들어갔다 (9장).

---

## 1. 기술 선택

- **언어/런타임**: TypeScript (strict)
- **렌더**: Canvas 2D. 스프라이트를 쓰되 **도형/색상만으로 그리는 경로를 폴백으로 남긴다.**
  시트를 하나라도 못 불러오면 `loadSprites()`가 `null`을 내고 전체가 도형 모드로 떨어진다 —
  절반만 그려진 화면이 아무것도 없는 화면보다 나쁘다
- **빌드**: Vite
- **테스트**: Vitest (시뮬레이션 코어는 DOM 없이 순수 테스트)
- **출시 경로**: itch.io(웹) → Tauri 래핑(Steam) → Capacitor(모바일)

### 선택 근거

최종 목표가 온라인 대전이다. 봄버맨류 온라인의 최대 난관은 서버 권위(authoritative) 동기화인데,
**클라이언트와 서버가 같은 시뮬레이션 코드를 실행**하면 이 문제가 대부분 사라진다.
TS는 브라우저와 Node에서 동일 코드가 돌아가므로 이 구조가 공짜다.

> 네이티브 모바일/콘솔이 1순위가 되면 Godot 4로 이주한다.
> 아래 2~5장(규칙·상수·상태 모델)은 스택과 무관하므로 그대로 재사용된다.

---

## 2. 핵심 설계 원칙

### 2.1 결정론적 시뮬레이션 (가장 중요)

```
step(state: GameState, inputs: InputFrame[]) -> GameState
```

- **고정 타임스텝 60Hz**. 렌더 프레임과 시뮬레이션 틱을 분리 (accumulator 패턴)
- **부동소수점 금지**. 모든 좌표·속도는 정수. `1 타일 = 1000 units`
- 난수는 시드 기반 PRNG(xorshift32)를 상태에 담아 전달. `Math.random()` 사용 금지
- `step`은 DOM, 타이머, I/O에 접근하지 않는다

> **결정론적이지만 순수 함수는 아니다.** `step`은 성능을 위해 상태를 제자리에서 변경한다.
> 롤백·리플레이에 필요한 스냅샷은 `cloneState()`로 명시적으로 뜬다.
> 매 틱 상태를 통째로 복사하는 것보다 이쪽이 넷코드에 유리하다 —
> 롤백은 어차피 "특정 시점의 스냅샷"만 필요하지 모든 틱의 사본이 필요하지 않다.

이걸 처음부터 지키면 아래가 전부 공짜로 따라온다:

| 얻는 것 | 방법 |
|---|---|
| 리플레이 | 시드 + 입력 로그만 저장 (수 KB) |
| 온라인 넷코드 | 서버가 같은 `step`을 권위 실행 / 클라 예측·롤백 |
| 버그 재현 | 입력 로그를 테스트 픽스처로 |
| AI 학습·평가 | 렌더 없이 초당 수만 틱 시뮬레이션 |

### 2.2 입력 인터페이스 통일

사람이든 AI 봇이든 네트워크 원격 플레이어든, **똑같은 `InputFrame`을 뱉는다.**

```ts
type InputFrame = {
  playerId: PlayerId;
  tick: number;
  move: Dir | null;      // Up | Down | Left | Right
  placeBubble: boolean;
};
```

봇이 `GameState -> InputFrame` 함수라면, 나중에 서버에 그대로 올릴 수 있다.
봇에게 렌더 정보나 특권을 절대 주지 않는다.

### 2.3 레이어 분리

```
[입력 소스]        [시뮬레이션]         [출력]
키보드 어댑터  ─┐                    ┌─ Canvas 렌더러
AI 봇         ─┼─→ step() → State ──┼─ 사운드
네트워크(나중) ─┘                    └─ 디버그 오버레이
```

렌더러는 State를 **읽기만** 한다. 보간(interpolation)은 렌더 레이어에서만 수행.

---

## 3. 게임 규칙

### 3.1 맵

- **15 x 13 타일** (가로 x 세로)
- 타일 종류:
  - `Empty` — 통과 가능
  - `Hard` — 파괴 불가. 맵 테두리 전체 + 내부의 `x % 2 === 0 && y % 2 === 0` 위치에 격자 배치
    (테두리를 포함하므로 짝수/짝수다. 스폰이 되는 홀수/홀수 좌표는 항상 열려 있다)
  - `Soft` — 물줄기로 파괴 가능. 파괴 시 확률적으로 아이템 드랍
- 스폰: 네 모서리. 각 스폰의 인접 3칸(L자)은 `Empty` 보장 — 시작하자마자 갇히는 상황 방지

### 3.2 물풍선 (Bubble)

- 플레이어당 동시 설치 개수 = `bubbleCapacity` (기본 1)
- 설치 후 **3초(180틱)** 뒤 폭발
- 물풍선은 다른 플레이어에게 **벽**이다. 단 **방금 자기가 놓은 물풍선 위에서는 빠져나올 수 있다** (봄버맨 표준)
  - 구현: **"지금 그 타일에 겹쳐 있는 플레이어에게만 뚫려 있다"** — 이 한 줄이 전부다.
    별도 상태를 저장하지 않는다. 자연히 아래가 전부 성립한다:
    자기 물풍선에서 걸어 나올 수 있고 / 벗어나면 다시 못 들어가고 /
    설치 순간 같은 타일에 있던 다른 플레이어도 갇히지 않는다

### 3.3 물줄기 (Water)

- 폭발 시 **십자(+) 4방향**으로 `power` 타일만큼 뻗음 (기본 1)
- 진행 규칙:
  - `Hard` 만나면 **그 앞에서 정지**
  - `Soft` 만나면 **그 블록을 파괴하고 정지** (뚫고 지나가지 않음).
    단 즉시 열리지 않고 `Breaking` 상태로 **여전히 막아둔다** — 아래 참조
  - `Breaking`(부서지는 중) 만나면 그 앞에서 정지. 아직 벽이다
  - 다른 물풍선을 만나면 **즉시 연쇄 폭발** (같은 틱에 처리 — BFS 큐로)
  - 아이템을 만나면 아이템 소멸
- 물줄기 지속 시간: **0.5초(30틱)**. 이 동안 닿는 플레이어는 트랩

#### 부서진 블록은 물줄기가 걷힐 때까지 막혀 있다 (`Tile.Breaking`)

플레이 중 발견된 문제다. 블록을 즉시 `Empty`로 바꾸면 이런 일이 생긴다:

```
[물풍선] [블록] [플레이어]     플레이어는 사거리 밖이라 안전하다
   ↓ 폭발
[물줄기] [열림] [플레이어]     블록이 사라지는 순간 빨려 들어간다
                              → 남아 있는 물줄기에 갇힌다
```

벽 반대편에서 벽을 밀고 있던 플레이어는 **예고도 반응 시간도 없이** 죽는다.
이 장르에서 가장 나쁜 종류의 죽음이다.

그래서 파괴된 블록은 `Breaking` 상태가 되어 **물줄기가 걷힐 때까지 계속 막는다.**
물줄기 수명이 곧 부서지는 애니메이션 시간이고, 물줄기가 사라지는 순간 `Empty`가 된다.
아이템은 파괴 시점에 굴려서 놓아두지만, 타일이 막혀 있는 동안에는 주울 수 없다.

원작 모작들이 파괴 애니메이션(`block_1_pop`, 5프레임)을 두는 이유가 이것이다.

### 3.4 물방울 트랩 — 이 게임의 정체성

봄버맨과 갈리는 지점. 물줄기에 맞아도 **즉사가 아니다.**

```
[정상] --물줄기 피격--> [트랩(물방울에 갇힘)] --5초 경과--> [사망]
                              │
                              ├── 아군이 물방울에 접촉 --> [정상] (즉시 구출)
                              ├── 아군의 물줄기가 물방울 명중 --> [정상] (즉시 구출)
                              └── 적팀이 물방울에 접촉 --> [사망] (즉시 터뜨림)

바늘을 갖고 있으면 애초에 갇히지 않는다 (1개 소모, 무적 1초)
```

#### 혼자 힘으로는 나올 수 없다

처음에는 방향키 연타로 탈출할 수 있게 했다. 결과는 **아무도 죽지 않는 게임**이었다.

| | 탈출까지 | 트랩 지속 |
|---|---|---|
| 사람 (초당 8회 연타) | 약 1.5초 | 5초 |
| hard 봇 (매 틱 전환) | **0.2초** | 5초 |

봇은 초당 60회 방향을 바꿀 수 있어서 사람보다 7배 빨리 빠져나왔다.
가두는 행위 자체가 무의미해졌고, 플레이어는 "죽었는데 연타하니 살아난다"고 느꼈다.

그래서 자력 탈출을 **없앴다.** 살아나는 길은 셋뿐이다:
**아군 접촉 / 아군의 물줄기 / 바늘 아이템.**

물줄기 구출도 소유자 무관에서 **아군 것만**으로 좁혔다.
적이 우연히 풀어주는 것은 "동료가 구한다"는 규칙과 어긋난다.

이 변경으로 게임의 성격이 바뀐다 — **갇히면 팀에 기대야 한다.**
개인전에서는 구해줄 사람이 없으므로 트랩이 사실상 사형 선고다.

> **물방울은 닿으면 터진다. 누가 터뜨렸는지가 결과를 가른다.**
> 적이 닿으면 죽고, 아군이 닿으면 산다. 둘 다 있으면 적이 이긴다
> (아군이 먼저 닿았다면 이미 풀려났을 것이므로).

> **구출은 소유자 무관이다.** 적이 쏜 물줄기여도 물방울에 닿으면 풀려난다(원작 룰).
> "적을 가둔 뒤 그 근처에서 물풍선을 함부로 못 터뜨린다"는 긴장감이 여기서 나온다.

#### 아군 접촉 구출을 넣은 이유 (플레이 테스트 결과)

처음에는 물줄기로만 구출할 수 있게 하고 아군 접촉은 "그냥 통과"로 두었다.
2v2를 실제로 해보니 **구출이 사실상 불가능했다.** 창이 이만큼밖에 없다:

```
트랩 지속 300틱(5초) − 물풍선 신관 180틱(3초) = 120틱 = 2초
```

갇힌 지 2초 안에 물풍선을 놓아야 하고, 파워 1이면 바로 옆 칸이어야 한다.
이미 옆에 서 있던 경우가 아니면 안 된다. 게다가 **달려가서 닿는 것**이
가장 직관적인 구조 동작인데 그게 무반응이었다.

규칙 표는 "동작한다"고 되어 있었지만 실제로는 아군에게 구조 수단이 없었다.
유닛 테스트로는 잡히지 않는 종류의 결함이다 — 규칙은 각각 정확히 동작했다.

- 트랩 지속: **5초(300틱)**. 시간 초과 시 사망
- **갇힌 직후 `RESCUE_GRACE`(=물줄기 수명) 동안은 구출 판정을 하지 않는다.**
  물줄기는 0.5초간 남아 있으므로, 이 유예가 없으면 나를 가둔 바로 그 물줄기가
  다음 틱에 나를 곧바로 풀어준다. 물줄기 수명만큼 기다리면 그 물줄기는 반드시 사라져 있다
- **자력 탈출은 없다** (위 참조). 남은 길은 아군 구출 / 물줄기 재명중 / 시간 초과 사망뿐
- 트랩 상태에서는 이동 불가, 물풍선 설치 불가
- 탈출/구출 직후 **1초 무적**
- 트랩된 플레이어의 타일은 다른 플레이어의 통행을 막지 않는다 (밟으러 들어가야 하므로)

### 3.5 아이템

`Soft` 블록 파괴 시 30% 확률로 드랍. 종류별 가중치:

| 아이템 | 효과 | 가중치 |
|---|---|---|
| 물풍선 | `bubbleCapacity += 1` (최대 8) | 25 |
| 물물약 | `power += 1` (최대 8) | 25 |
| 롤러 | `speedLevel += 1` (최대 5) | 20 |
| 바늘 | 트랩 시 1회 즉시 탈출 (스택 가능) | 10 |
| 물약(초강화) | `power`를 일시적으로 최대치로 (15초) | 5 |
| 해골 | 랜덤 디버프 15초 (속도 저하 / 물풍선 감소 / 강제 설치) | 5 |
| 방패 | 5초 무적 | 10 |

**일시 효과는 원래 능력치를 건드리지 않는다.** 물약·해골은 남은 틱만 `Player`에 들고
있다가 `stats.ts`가 계산 시점에 덮어쓴다(`effectivePower`, `effectiveCapacity`,
`effectiveSpeed`). 효과가 끝날 때 "원래 값으로 되돌리는" 코드가 없으므로,
효과가 겹치거나 중간에 아이템을 더 먹어도 값이 어긋나지 않는다.
물풍선의 위력은 **설치 시점에 고정**되므로 물약이 끊겨도 이미 놓은 것은 그대로 터진다.

부순 블록에서 나온 아이템은 **그 폭발에는 지워지지 않는다.** 물줄기가 걷힌 뒤
드러나는 것이 원작의 순서다. 이후의 다른 폭발에는 지워진다.

### 3.6 팀 (확정: 지원함)

모든 플레이어는 `teamId`를 가진다. **개인전은 "전원이 서로 다른 팀"인 특수 케이스**로 처리한다.
별도 분기를 만들지 않는다.

| 상호작용 | 아군 | 적 | 자기 자신 |
|---|---|---|---|
| 물줄기 피격 → 트랩 | **된다** (프렌들리 파이어 ON) | 된다 | **된다** (자멸 가능) |
| 물방울에 물줄기 명중 → 구출 | 된다 | **된다** (소유자 무관) | 된다 |
| 물방울에 접촉 | **구출** | **제거** | — |

- 프렌들리 파이어를 켜두는 이유: 팀전에서 아군이 아무 위험이 아니면 좁은 통로 플레이가 사라진다
- 갇힌 사람은 남을 구할 수도, 밟을 수도 없다
- 팀 판정이 필요한 곳은 접촉 처리 한 군데뿐이라, 실제 팀 로직은 `systems/trap.ts`에만 있다

### 3.7 승리 조건

- **마지막 생존 팀**이 승리 (`alive`인 플레이어들의 `teamId`가 1종류만 남았을 때)
- 전원 동시 사망 시 무승부
- **서든데스**: 3분 경과 시 바깥쪽부터 나선형으로 `Hard` 블록이 떨어져 맵을 좁힘. 맞으면 즉사

---

## 4. 수치 상수 (튜닝 대상)

```ts
export const TICK_RATE   = 60;
export const TILE        = 1000;  // sub-units per tile

// 이동 속도 (units per tick) — TILE=1000, 60Hz 기준
export const SPEED_TABLE = [40, 46, 52, 58, 64, 70]; // 2.4 ~ 4.2 타일/초

// 히트박스: 타일보다 작게 잡아야 조작감이 산다
export const PLAYER_HITBOX = 800;  // 0.8 타일
export const CORNER_ASSIST =  60;  // 수직 보정 속도 (units/tick)

export const BUBBLE_FUSE     = 180; // 3.0초
export const WATER_DURATION  =  30; // 0.5초
export const TRAP_DURATION   = 300; // 5.0초
export const INVULN_DURATION =  60; // 1.0초
export const SUDDEN_DEATH_AT = 60 * 180; // 3분
```

### 히트박스는 그림 크기와 맞물린다

처음엔 0.6타일(31px)로 잡았는데, 캐릭터 그림이 64px이라 **벽에 딱 붙어도
그림이 16px씩 벽 안으로 파묻혔다** — 타일의 31%다. 조작은 멀쩡한데 눈에는 어긋나 보인다.

참고 프로젝트는 충돌 박스(20px)와 별개로 `_BODY_WIDTH = 42`(0.81타일)라는
"몸통" 크기를 따로 들고 있었다. 그 값에 맞춰 0.8타일로 올렸다.

물줄기 피격은 **중심 타일**로 판정하므로 히트박스를 키워도 회피 난이도는 그대로다.
바뀌는 것은 벽에 닿는 시점과 통로를 스치고 지나갈 때의 여유뿐이다.

### 조작감의 핵심: 수직축 정렬

세로로 이동하면 히트박스(0.6타일)가 **두 행에 걸친다.** 그 상태에서 가로를 누르면
벽 모서리에 스쳐 막힌다. 격자 게임이 답답하게 느껴지는 원인이 대부분 이것이다.

규칙은 하나다:

> 축 이동 중에는 수직 좌표를 **"갈 수 있는 레인"의 중심**으로 끌어당긴다.
> - 레인 중심에서 `STRADDLE_MARGIN`(250) 넘게 벗어나 있고,
>   **지금 레인이 진행 방향으로 막혔는데 옆 레인은 열려 있으면** → 옆 레인으로 (코너 어시스트)
> - 그 외에는 → 지금 레인 중심으로 (자동 정렬)

**"두 레인에 걸쳤는가"를 히트박스로 판정하면 안 된다.** 처음엔 그렇게 만들었는데,
히트박스를 0.6에서 0.8타일로 키우자 걸침 기준이 200에서 100으로 반토막 나면서
**통로에서 살짝만 어긋나도 옆 행으로 끌려갔다.** 히트박스는 충돌용이고,
코너 보정의 예민함은 별개의 값이어야 한다.

조건을 "한쪽만 열림"에서 **"지금 레인이 막혔을 때만"** 으로 좁힌 것도 같은 이유다.
지금 레인으로 갈 수 있는데 옆 레인이 열렸다고 굳이 옮겨갈 이유가 없다.

처음엔 코너 어시스트와 자동 정렬을 별개 함수로 나눴는데,
그러면 코너를 돈 뒤 레인 중심에 못 미친 채(중심에서 180 units 벗어난 위치) 멈춘다.
막히지 않게 된 순간 보정이 꺼지기 때문이다. 두 규칙을 하나로 합쳐야 정확히 안착한다.

`CORNER_ASSIST`는 이 보정 속도다. 걷는 속도(40)보다 약간 빠르게 잡아야
"코너를 돌아 걸어간다"고 느껴진다. 너무 크면 순간이동처럼 보인다.

**단, 빠른 보정은 코너를 돌 때만 쓴다.** 그냥 레인에 안착하는 경우까지 빠르게 하면
보정이 이동을 앞지른다. 실제로 그렇게 만들었더니, 방향을 매 틱 번갈아 누르는 입력에서
전진 40이 보정 60에 되밀려 **플레이어가 스폰 타일에 못 박힌 채 한 발짝도 못 나갔다.**
통합 테스트가 아니었으면 못 잡았을 버그다.

### 이 게임은 레인 고정이다

보정 속도를 걷는 속도와 같게 맞춰도, 방향을 매 틱 번갈아 누르면 전진과 보정이
1:1로 상쇄되어 제자리에 머문다. **이건 버그가 아니라 장르의 정상 동작이다.**
봄버맨/크레이지아케이드에서 플레이어는 항상 어느 한 레인 위에 있고,
대각선 위치에 존재할 수 없다. 오른쪽으로 가다 아래를 누르면 먼저 열 중심으로 끌려간다.

따라서 **자동화된 입력(테스트·AI 봇)은 방향을 일정 시간 유지해야 한다.**
매 틱 랜덤 방향을 뽑는 입력기는 이 게임에서 의미 있는 부하가 되지 못한다.

---

## 5. 상태 모델

```ts
type GameState = {
  tick: number;
  rng: number;                  // xorshift32 시드
  phase: Phase;                 // Playing | SuddenDeath | Over (0|1|2)
  width: number; height: number;
  map: Uint8Array;              // width*height, Tile
  players: Player[];
  bubbles: Bubble[];
  waters: Water[];
  items: Item[];
  nextBubbleId: number;         // Bubble id 발급용
  suddenDeathIndex: number;     // 나선 순서상 다음에 블록이 떨어질 위치
  winnerTeamId: TeamId | null;  // 진행 중이면 null, 무승부면 -1
};

type Player = {
  id: PlayerId;
  teamId: TeamId;               // 개인전 = 전원 서로 다른 값
  alive: boolean;
  x: number; y: number;         // sub-unit 좌표. 중심점
  facing: Dir;
  status: PlayerStatus;         // Normal | Trapped | Invulnerable
  statusTicks: number;          // 남은 틱
  bubbleCapacity: number;
  bubblesPlaced: number;
  power: number;
  speedLevel: number;
  needles: number;
  // 일시 효과는 남은 틱만 들고 있다가 stats.ts가 계산 시점에 덮어쓴다 (3.5 참조)
  potionTicks: number;
  skullTicks: number;
  skullKind: SkullKind;
};

type Bubble  = { id, ownerId, tx, ty, fuse, power };
type Water   = { tx, ty, ticksLeft, ownerId, kind: WaterKind, dir: Dir | null };
type Item    = { tx, ty, kind };
```

- `map`은 `Uint8Array` — 상태 스냅샷/전송이 값싸다
- 엔티티는 배열. id는 단조 증가 정수. 삭제는 swap-remove
- 열거값은 전부 const 객체 + 정수다. 문자열 유니온을 쓰지 않는 이유는 스냅샷을
  JSON으로 통째로 보내기 때문이다 (8.3 참조)
- `Water.dir`은 렌더링 전용이다. 물줄기 그림이 뻗어나간 방향으로 갈리는데,
  타일 좌표만으로는 그걸 복원할 수 없다

---

## 6. 디렉터리 구조

```
crazy/
├─ DESIGN.md  README.md
├─ package.json            (npm workspaces)
├─ fly.toml                # 서버 배포 (8.8)
├─ packages/
│  ├─ core/                # 순수 TS. DOM 의존 0. 시뮬레이션 전부
│  │  ├─ src/
│  │  │  ├─ types.ts
│  │  │  ├─ constants.ts
│  │  │  ├─ rng.ts
│  │  │  ├─ geometry.ts    # 타일 ↔ sub-unit 좌표 변환
│  │  │  ├─ map.ts         # 맵 생성 / 타일 질의
│  │  │  ├─ stats.ts       # 일시 효과를 얹은 실효 능력치 (3.5)
│  │  │  ├─ sim.ts         # step() 진입점
│  │  │  ├─ net.ts         # 스냅샷 직렬화 + 프로토콜 타입 (8.5)
│  │  │  ├─ replay.ts      # 시드 + 입력 로그 (7.6)
│  │  │  ├─ systems/
│  │  │  │  ├─ movement.ts    # 이동 + 충돌 + 코너 어시스트
│  │  │  │  ├─ bubble.ts      # 설치 / 신관
│  │  │  │  ├─ explosion.ts   # 물줄기 전파 + 연쇄
│  │  │  │  ├─ trap.ts        # 트랩 / 구출 / 밟기
│  │  │  │  ├─ item.ts        # 드랍 / 획득
│  │  │  │  ├─ suddenDeath.ts # 나선형 블록 낙하
│  │  │  │  └─ victory.ts     # 승패 판정
│  │  │  └─ ai/
│  │  │     ├─ bot.ts         # GameState -> InputFrame
│  │  │     ├─ danger.ts      # 위험 맵 (폭발 예정 타일 + 남은 틱)
│  │  │     └─ pathfind.ts    # 시간 인지 BFS
│  │  └─ test/
│  ├─ client/              # 브라우저
│  │  └─ src/
│  │     ├─ main.ts        # 게임 루프 (accumulator) + 설정 UI
│  │     ├─ render.ts      # Canvas 2D. 스프라이트 + 도형 폴백
│  │     ├─ sprites.ts     # 시트 규격표 / 로더 / drawFrame
│  │     ├─ characters.ts  # 자리 → 캐릭터 배정
│  │     ├─ input.ts       # 키보드 · 터치 -> InputFrame
│  │     ├─ online.ts      # WebSocket + 예측/재조정 (8.4)
│  │     ├─ events.ts      # 상태 차이 -> 이벤트
│  │     ├─ audio.ts       # 샘플 재생 + 합성음 폴백
│  │     ├─ particles.ts   # 파티클
│  │     └─ corpses.ts     # 사망 연출
│  └─ server/              # Node + ws. core의 step()을 60Hz 권위 실행
│     ├─ src/index.ts      # WebSocketServer + /health
│     └─ src/room.ts       # 방 하나 = 한 판
```

**`client`는 `test/` 대신 `src/` 안에 시험을 둔다.** tsconfig가 `rootDir`을 `src`로
잡고 있어 형제 디렉터리를 넣을 수 없다. `core`와 관례가 다른 이유는 그것뿐이다.

**규칙: `core`는 `client`를 절대 import하지 않는다.** 이 한 줄이 온라인 확장의 전제조건.

---

## 7. AI 봇 설계

난이도별로 같은 프레임워크, 파라미터만 다르게. (`core/src/ai/`)

1. **위험 맵** (`danger.ts`) — 각 타일이 **몇 틱 뒤에** 물줄기에 덮이는가.
   "지금 위험한가"가 아니라 "언제 위험해지는가"여야 놓고 빠져나올 수 있는지 판단된다.
   연쇄 폭발까지 고정점 반복으로 전개한다 — 남의 물풍선이 내 물풍선을 앞당겨 터뜨린다.
   **정지 규칙이 `explosion.ts`와 정확히 같아야 한다.** 어긋나면 봇이 헛다리를 짚는다
2. **시간 인지 BFS** (`pathfind.ts`) — 거리만으로는 부족하다.
   "도착했을 때 그 타일이 안전한가"를 같이 봐야 물줄기 속으로 걸어 들어가지 않는다.
   그래서 통행 판정이 도착 예정 틱을 함께 받는다
3. **행동 우선순위** (`bot.ts`) — 갇히면 할 수 있는 게 없으므로 목록에 없다
   1. 위험하면 **도망** — 다른 건 생각하지 않는다
   2. 놓을 만하고 **탈출 경로가 있으면** 설치
   3. **갇힌 아군 구조** → 아이템 → 부술 블록 옆 → 적 쪽 순으로 이동.
      구조가 아이템보다 앞인 이유: 아군은 5초 안에 죽지만 아이템은 도망가지 않는다
4. **탈출 경로 확인** — 가상의 물풍선을 포함한 위험 맵을 만들고, 폭발 전에 닿을 수 있는
   "폭발 예정이 전혀 없는" 타일이 있는지 본다. **이 검사 하나가 봇 품질의 대부분이다**
5. **계획은 타일이 바뀔 때만 새로 세운다.** 매 틱 새 방향을 뽑으면 레인 정렬이
   전진을 상쇄해 제자리에서 굳는다(2.1의 레인 고정 참조).
   못 움직인 채 12틱이 지나면 교착으로 보고 다시 계획한다
6. **난이도** — `reactionTicks`(위험을 알아차리기까지의 지연, 0/8/20),
   `aggression`(설치 적극성), `recklessness`(탈출 확인을 건너뛸 확률)

   자력 탈출이 있던 시절에는 연타 속도가 난이도의 대부분이었다. 그걸 없애자
   **hard와 easy의 승률이 11:12로 동전 던지기가 됐다.** 설치 적극성만으로는
   차이가 나지 않았기 때문이다. 갇히면 곧 죽음인 지금은
   **"얼마나 늦게 도망치는가"가 곧 생존율**이라 `reactionTicks`가 핵심 손잡이다.

   난이도는 따로 돌린 경기 길이가 아니라 **같은 판에 섞어 넣고 승률로** 재야 한다.
   경기 길이는 실력이 아니라 결판 속도를 잰다 — 센 봇끼리 붙으면 오히려 빨리 끝난다.

### 봇이 제자리에서 떨리던 문제

플레이 중 "가만히 서서 좌우로 덜덜 떠는" 봇이 보였다. 측정해 보니 이랬다:

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 초당 방향 전환 | 11.2회 | **1.82회** |
| 초당 역방향 전환 | 9.71회 | **0.96회** |
| 초당 이동 타일 | 11.92 | 2.9 |

**방향 전환의 87%가 정반대로 꺾는 것**이었다. 원인은 둘이다:

1. 갈 곳이 없을 때 **매번 새 랜덤 방향**을 뽑았다. 게다가 막힌 방향도 골라서
   못 움직이고 12틱마다 다시 뽑기를 반복했다
2. **타일을 넘을 때마다 목표를 다시 계산**해서, 경계에서 왔던 길로 되돌아가기를 반복했다

고친 방법: 배회할 때는 **열린 방향만** 고르고 25~60틱 유지한다(`holdTicks`).
그리고 막혀 있는 게 아니라면 **정반대로 꺾지 않는다**.

`초당 이동 타일`이 11.92에서 2.9로 준 것은 나빠진 게 아니다. 기본 이동 속도가
2.4타일/초인데 12가 나왔다는 것 자체가 **경계를 오가며 진동한 횟수가 부풀린 값**이었다.
지금 값이 실제 이동 속도와 맞는다.

---

## 7.5 소리·파티클·사망 연출

**시뮬레이션은 이벤트를 남기지 않는다.** 남기면 결정론과 스냅샷이 복잡해지고,
서버도 쓸 일 없는 데이터를 나르게 된다. 대신 클라이언트가 **상태를 두 시점 비교**해서
무슨 일이 일어났는지 읽어낸다 (`events.ts`).

```
새 물풍선 id      → 설치
새 Center 물줄기  → 폭발
Soft → Breaking   → 블록 파괴
능력치 총합 증가   → 아이템 획득
status 전이       → 갇힘 / 구출
alive true→false  → 사망
```

온라인에서도 그대로 동작한다. 스냅샷도 결국 같은 `GameState`이기 때문이다.

**소리는 Web Audio로 합성한다** (`audio.ts`). 음원 파일을 쓰지 않는 이유는 저작권이다 —
`public/sounds/`에 음원 파일이 있으면 그것을 쓰고, 없으면 합성음으로 대신한다.
스프라이트와 같은 계약이라 파일이 없어도 게임은 그대로 돌아간다.
다만 스프라이트와 달리 **종류별로** 대체한다 — 일부만 음원이어도 화면처럼 어색해지지 않는다.

파일 이름은 원작 것이 아니라 **이벤트 이름을 따른다.** 어떤 출처의 음원이든
이름만 맞추면 꽂히게 하기 위해서다:

```
sounds/bubble   설치      sounds/trap     갇힘
sounds/explode  폭발      sounds/rescue   구출
sounds/break    파괴      sounds/death    사망
sounds/pickup   아이템    sounds/bgm      배경음 (반복)
```

`.mp3` → `.wav` → `.ogg` 순으로 찾는다.

**사망 연출** (`corpses.ts`) — 시뮬레이션에서 죽은 플레이어는 즉시 `alive: false`가 되어
사라진다. 규칙상 맞지만 그냥 없어지면 무슨 일이 일어났는지 보이지 않는다.
쓰러지는 13프레임을 재생하고, 점멸시킨 뒤 사라지게 한다.

파티클·시체·소리 모두 **시뮬레이션이 진행된 틱에만** 갱신한다.
렌더 주사율에 묶으면 화면이 빠른 기기에서 효과가 더 빨리 사라진다.

## 7.6 리플레이

**시드와 입력 로그만 저장한다.** 시뮬레이션이 결정론적이라 상태를 저장할 필요가 없다 —
같은 시드로 시작해 같은 입력을 같은 순서로 먹이면 반드시 같은 결과가 나온다.

입력은 **바뀌는 순간만** 담는다. 매 틱 4명을 다 담으면 1분에 14,400줄이지만,
변경분만 담으면 3,000줄 아래로 떨어진다. 다만 **물풍선 설치는 예외**다 —
1틱짜리 신호라 "바뀐 게 없어도" 반드시 기록해야 한다.
이동은 다음 변경까지 유지되고 설치는 그 틱에만 켜지는 비대칭이 여기서 나온다.

부수적으로 **결정론 회귀 테스트가 공짜로 생긴다.** 봇 대전을 녹화하고 재생해서
최종 상태(맵·플레이어·엔티티·rng 시드까지)를 통째로 비교한다.
어딘가에서 결정론이 깨지면 이 테스트가 먼저 잡는다.

온라인에서는 녹화하지 않는다. 클라이언트가 아는 것은 자기 입력뿐이라
남의 입력을 담을 수 없다. 서버에서 녹화하는 것이 맞는 자리다.

## 8. 온라인 설계 (M8)

### 8.1 이미 갖춰진 전제

여기까지의 구조적 선택이 전부 이걸 위한 것이었다. 새로 만들 게 아니라 **연결만 하면 된다**:

| 전제 | 어디에 | 왜 필요한가 |
|---|---|---|
| 결정론적 `step()` | `core/sim.ts` | 서버와 클라가 같은 결과를 내야 한다 |
| 정수 좌표 · 시드 PRNG | `constants.ts`, `rng.ts` | 기기가 달라도 결과가 갈리지 않는다 |
| `InputFrame` 통일 | `types.ts` | 원격 플레이어를 봇·사람과 똑같이 다룬다 |
| `core`가 `client`를 모름 | 규칙 | Node에서 그대로 실행된다 |
| `cloneState()` | `sim.ts` | 롤백 지점을 뜬다 |

### 8.2 구조

```
[클라 A] ──입력──┐
[클라 B] ──입력──┼──> [서버: 방 하나 = step() 60Hz 권위] ──스냅샷──> 전원
[클라 C] ──입력──┘                    ↑
[빈 자리]                        봇이 그대로 채운다
```

빈 자리를 봇이 채울 수 있는 이유는 `botInput()`이 `GameState -> InputFrame` 순수 함수라서다.
서버에서 사람과 봇을 구분할 필요가 없다.

### 8.3 스냅샷을 통째로 보낸다

| | 크기 |
|---|---|
| 이론값 (map 195 B + players ~160 B + 엔티티 ~200 B) | 약 500 B |
| **JSON 실측** | **1,712 B** |

JSON 키 이름 때문에 3배 넘게 부풀지만, 20Hz면 클라이언트당 **34 KB/s**(0.27 Mbps)다.
4명이 붙어도 서버 업링크 137 KB/s. **델타 압축도 비트패킹도 만들지 않는다.**
상태가 작고 격자라서 가능한 일이고, 봄버맨류가 FPS보다 넷코드가 쉬운 이유이기도 하다.
처음부터 압축을 넣으면 "규칙이 틀린 것"과 "직렬화가 틀린 것"을 구분할 수 없게 된다.

### 8.4 세 단계로 쪼갠다

**1단계 — 예측 없이 서버 권위** ✅

클라는 입력만 보내고 받은 스냅샷을 그리기만 한다. 원격 플레이어는 스냅샷 사이를 보간한다.
입력이 왕복 지연만큼 늦게 반영되지만 RTT 80ms 이하면 이 장르에선 견딜 만하다.
**구현량이 3분의 1이고, 여기서 규칙 동기화가 맞는지부터 확인한다.**

**2단계 — 자기 캐릭터 예측 + 재조정** ✅

자기 입력은 즉시 로컬에 반영하고, 스냅샷이 도착하면 그 상태로 되감아
**아직 반영되지 않은 입력만** 다시 재생한다. 어디까지 반영됐는지 알아야 하므로
입력마다 번호(`seq`)를 붙이고 서버가 스냅샷에 `ack`로 돌려준다.
입력을 매 틱 보내는 이유가 이것이다 (초당 2.4KB — 아껴서 얻는 것보다 잃는 게 크다).

**원격 플레이어는 예측하지 않는다.** 그들의 입력을 알 수 없어 추측이 빗나가면
오히려 더 어색해지고, 스냅샷 보간만으로도 충분히 부드럽다.

측정값 (localhost, 5초):

```
스냅샷 99개, ack 195 / 보낸 입력 197개, 미반영 버퍼 2개
보정량  중앙값 0 · 평균 7.7 · 최대 80 units      (1타일 = 1000 units)
```

**중앙값 0** — 대부분의 재조정에서 예측이 서버와 비트 단위로 일치한다.
최대 80은 정확히 `2틱 × 40 units`로, 방향을 바꾼 직후 서버가 아직 새 입력을
반영하지 않은 만큼이다. 결정론적 `step()`이 실제로 값을 하는 지점이다.

이 보정량은 HUD에 노출된다. 실제 네트워크에서 계속 큰 값이 뜬다면
예측이 깨진 것이므로 가장 먼저 볼 숫자다.

**3단계 — 방 코드 / 재접속 / 관전** ✅

- 접속 주소에 `?room=CODE`. 코드는 대문자·숫자 8자로 정규화하고, 없으면 `MAIN`
- 방은 첫 접속 때 생기고 아무도 없으면 사라진다. 방마다 독립된 `step()` 루프가 돈다
- **자리가 꽉 차면 거절하지 않고 관전으로 받는다.** 관전자는 스냅샷만 받고 입력은 무시된다
- 끊기면 지수 백오프(1→2→4→8초, 5회)로 자동 재접속.
  **같은 자리로 돌아간다는 보장은 없다** — 서버가 빈 자리를 순서대로 주기 때문이다.
  자리를 지키려면 토큰이 필요한데, 지금 규모에서는 과하다
- `GET /health`로 방과 인원을 확인할 수 있다 (배포 헬스체크 겸용)

### 8.8 배포

```
클라이언트   https://scenery67.github.io/Crazy/     GitHub Pages (정적)
서버         wss://<앱>.fly.dev                      Fly.io (Node)
```

**서버는 반드시 TLS여야 한다.** Pages는 https이고, 브라우저는 https 문서에서
`ws://` 평문 연결을 mixed content로 차단한다. Fly·Railway·Render는 도메인에
TLS를 자동으로 붙여주므로 이 문제가 없다 — 직접 VM에 올리면 인증서를 따로 잡아야 한다.

클라이언트는 빌드 시 `VITE_SERVER_URL`로 서버 주소를 주입받는다.
없으면 접속 중인 호스트를 가리켜서 로컬 개발이 그대로 된다.

### 8.5 프로토콜

WebSocket. **JSON으로 시작한다** — 눈으로 읽히는 게 초기 디버깅에서 압도적으로 유리하다.

타입은 `core/src/net.ts`에 있다. 서버와 클라가 같은 정의를 import한다.

```
클라 → 서버   { t: 'input', seq, move, place }              매 틱
서버 → 클라   { t: 'snapshot', state, ack }                 20Hz (SNAPSHOT_EVERY_TICKS = 3)
접속 시       { t: 'welcome', playerId | null, room, snapshotIntervalMs, state }
거절          { t: 'reject', reason }
```

`playerId`가 `null`이면 관전이다. `ack`는 서버가 마지막으로 반영한 `seq`이고,
클라는 이 번호 이후의 입력만 다시 재생한다 (8.4의 2단계).
`state`는 `SerializedState` — `map`만 `Uint8Array`에서 `number[]`로 바뀐 것이고
나머지는 `GameState` 그대로다.

### 8.6 core에 추가로 필요한 것

- 상태 직렬화 / 역직렬화 (`map`은 배열 또는 base64)
- 클라 예측용 입력 버퍼

**규칙 코드는 한 줄도 바뀌지 않는다.** 이게 설계가 맞았다는 증거다.

### 8.7 미리 짚어둘 위험

- **결정론 점검** — 부동소수점은 이미 없다. `Set`/`Map` 순회는 삽입 순서라 안전하고,
  엔티티는 전부 배열이라 순서가 고정된다. 좌표 최대값이 15,000이라 int32를 넘지 않는다.
  다만 새 시스템을 추가할 때마다 이 목록을 다시 확인해야 한다
- **밟기 판정이 지연에 민감하다** — 같은 타일에 있는지로 삶과 죽음이 갈린다.
  지연 보상을 넣지 말고 서버 권위로 단순하게 처리한다. 억울한 죽음보다
  "내 화면에서 죽었는데 살아 있는" 쪽이 덜 나쁘다
- **호스팅** — GitHub Pages는 정적이라 서버를 올릴 수 없다.
  클라이언트는 Pages에 두고 `wss://`로 별도 서버에 붙인다 (Fly.io / Railway / Durable Objects)
- **에셋 저작권** — 현재 스프라이트는 원작에서 추출한 것을 그대로 쓰고 있고,
  저장소와 배포본에 함께 올라간다. 공개 재배포이므로 테이크다운 대상이 될 수 있다.
  계속 공개할 생각이면 직접 제작하거나 CC0 에셋으로 갈아끼워야 한다

## 9. 구현 로드맵

| 단계 | 산출물 | 검증 방법 |
|---|---|---|
| ~~**M1** 골격~~ | ✅ 워크스페이스, 타입, 상수, 맵 생성, 빈 `step`, Canvas에 맵 렌더 | 맵이 화면에 보인다 |
| ~~**M2** 이동~~ | ✅ 이동 + 충돌 + 수직축 정렬 + 키보드 2인 입력 | 답답하지 않게 돌아다닐 수 있다 |
| ~~**M3** 폭발~~ | ✅ 물풍선 설치·신관·십자 전파·연쇄·블록 파괴 | 연쇄 폭발 유닛 테스트 |
| ~~**M4** 트랩~~ | ✅ 피격→트랩→탈출/구출/밟기/사망, 승리 조건 | 1인 자멸 시나리오 테스트 |
| ~~**M5** 아이템~~ | ✅ 드랍 테이블, 획득, 일시 효과, 해골 디버프 | 밸런스 확인 |
| ~~**M6** 봇~~ | ✅ 위험맵, 시간 인지 BFS, 자살 방지 설치 판정, 난이도 3종 | 봇 vs 봇 헤드리스 대전 |
| ~~**M7** 다듬기~~ | ✅ 서든데스·사운드·파티클·사망 연출·리플레이·모바일 터치 | 실제로 재미있는가 |
| ~~**M8** 온라인~~ | ✅ `packages/server`, 서버 권위 → 클라 예측 → 방·관전·재접속 (8장) | 두 브라우저로 같은 판 |

**로드맵은 여기까지다.** 다음에 무엇을 할지는 10장에 열어두었다.

M6의 **봇 vs 봇 헤드리스 대전**이 중요하다. 렌더 없이 시뮬만 돌려
"평균 게임 길이", "자멸 비율", "아이템 획득 분포"를 뽑으면 밸런스를 정량적으로 잡을 수 있다.

### 헤드리스 대전에서 나온 숫자

10판 × 난이도 3종, 최대 300초:

| | 평균 시간 | 종료 | 놓은 물풍선 | 부순 블록 |
|---|---|---|---|---|
| easy | 155초 | 10/10 | 110 | 69 (전부) |
| normal | 178초 | 10/10 | 155 | 69 (전부) |
| hard | 188초 | 10/10 | 161 | 69 (전부) |

**서든데스를 넣기 전에는 hard 봇끼리 0/10 종료였다.** 봇이 블록을 다 부수고 나면
서로 잘 피해다녀서 2분이 지나도 결판이 나지 않는다. 서든데스는 연출이 아니라
경기를 끝내는 유일한 장치다 — 실력이 붙을수록 없으면 안 된다.

무승부 비율이 40% 정도로 높다(마지막 둘이 같은 틱에 죽음). 4인 개인전 + 서든데스의
자연스러운 결과라 그대로 두었지만, 팀전 위주로 갈 경우 다시 볼 값이다.

### M6 시작 전에 알아둔 것 (통합 테스트에서 관측됨)

> 아래는 자력 탈출이 남아 있던 시절의 관측이다. 지금은 갇히면 아군만이 꺼내줄 수 있다.

방향을 20~60틱씩 유지하며 무작정 돌아다니는 입력기는 **8초 안에 전원 자멸한다.**
자기 물풍선에 갇히기 때문이다. 여기서 얻은 결론 하나는 지금도 유효하다:

**설치 전에 탈출 경로를 확인해야 한다** — 놓고 나서 살아나갈 길이 있을 때만 놓는다.
자력 탈출이 사라진 지금은 오히려 이 검사 하나에 봇의 생존이 전부 달려 있다.

또한 봇은 이동할 때 **방향을 유지해야 한다.** 매 틱 새 방향을 뽑으면
레인 정렬이 전진을 상쇄해서 그 자리에서 굳는다.

---

## 10. 미결정 사항

- [x] ~~팀전(2v2) 지원 여부~~ → **지원함.** `teamId`를 초기 모델에 포함 (3.6 참조)
- [x] ~~캐릭터 선택~~ → **구현함.** 설정 바에서 배찌/디즈니 × 빨강/파랑 넷 중 하나를 고른다.
      2인 로컬에서는 2P도 따로 고르고, 둘이 같은 것을 골라도 뒷사람이 다음 번호로 밀린다.
      배정은 `client/src/characters.ts`에 있다. **지금은 겉모습뿐이다** — 아래 참조

- [ ] **캐릭터별 고유 능력** — 참고 모작에서는 시작 능력치와 **상한이 캐릭터마다 다르다**:

  | | 물풍선 | 물줄기 | 속도 | 최대 물풍선 | 최대 물줄기 | 최대 속도 |
  |---|---|---|---|---|---|---|
  | bazzi | 1 | 1 | 5 | 6 | 7 | 9 |
  | dizni | 2 | 2 | 4 | 7 | 8 | 9 |

  "느리지만 물풍선을 많이 놓는" 식의 트레이드오프다. 우리는 **고르는 것이 외형뿐이고
  능력치는 전원 동일**(1/1/최대 8·8·6)이다.
  넣는다면 `Player`에 시작값·상한을 실어야 하고, `stats.ts`가 상한을 참조하도록 바꿔야 한다.
  캐릭터가 이미 자리마다 갈려 있으므로 붙일 자리는 마련되어 있다

- [ ] **캐릭터 그림의 비대칭** — 0번(빨강 배찌)만 원본에 방향별 큰 시트가 있어
  1:1로 선명하게 그려지고, 나머지 셋은 44x62 시트를 1.3배로 늘려 쓴다.
  게다가 갇힘·사망은 0번도 `chars/*` 계열이라 **빨강 배찌는 자기 자신과도 어긋난다**
  (걸을 때만 다른 시트).

  맞추는 길은 둘 — 나머지 셋의 방향별 시트를 만들거나, `sp.hero`를 버리고 넷을
  `chars/*`로 통일하거나. 후자가 코드는 훨씬 단순해지지만 제일 좋은 그림을 잃는다

- [ ] **온라인에서 고른 캐릭터가 전달되지 않는다** — 자리 번호가 곧 캐릭터라
  내 선택은 접속하는 순간 무시된다. 넣으려면 `welcome`만으로는 안 된다:
  `Room.join()`이 가장 낮은 빈자리를 주고 재접속 시 자리가 바뀌며, 관전자도 받아야 하므로
  **별도 브로드캐스트 메시지**가 필요하다. `Player`에 실는 쪽은 더 나쁘다 —
  `replay.test.ts`가 `players`를 통째로 비교하므로 겉모습 값이 결정론 검증에 끼어든다.
  지금 규모에서는 과하다

- [ ] **맵 레이아웃** — 참고 모작은 **15 × 11**에 손으로 그린 고정 레이아웃을 쓴다
  (`0`=빈칸, `1`=파괴 가능, `2`=파괴 불가). 우리처럼 15×13에 짝수/짝수 격자를 까는
  봄버맨 방식이 **아니다.** 파괴 불가 블록이 맵당 네 개뿐이고 나머지는 전부 손 배치다.

  즉 크레이지아케이드의 맵은 절차 생성이 아니라 **디자인된 것**이다.
  지금의 시드 생성은 유지하되, 고정 레이아웃을 텍스트로 싣는 경로를 열어두는 쪽이 맞아 보인다
- [x] ~~모바일 터치 조작~~ → **구현함.** 격자 4방향 이동이라 스틱은 밀어낸 방향을
      **가장 큰 축 하나로 스냅**한다. 대각선을 허용하면 레인 정렬과 싸우게 되어
      오히려 조작이 나빠진다.

      **띄울지 판정은 `(pointer: coarse)`로 한다.** `maxTouchPoints > 0`으로 하면
      터치스크린 달린 PC에서도 조작판이 떠버린다 — 그건 "터치가 되는가"이지
      "주 입력장치가 손가락인가"가 아니다. 판정이 애매한 기기를 위해
      실제 손가락이 닿으면 그때 띄우고, `T` 키로 수동 전환도 된다

- [ ] **아무도 안 쓰는 에셋이 약 3.7MB** — `public/`은 통째로 Pages에 올라가므로
  코드가 참조하지 않아도 배포된다. 8.7의 저작권 노출을 그만큼 넓히는 셈이다.
  아래는 코드에서 한 번도 불리지 않는 것들이다 (관측 사실이며, 처분은 아직 정하지 않았다):

  | | 파일 | 암시하는 것 |
  |---|---|---|
  | 화면 배경 | `login_scene_bg`(1.45MB) · `mode_select_scene_bg`(1.16MB) · `lobby_scene_bg`(915KB) · `play_bg` · `black_bg` | 로그인 → 로비 → 모드선택 흐름. 지금은 바로 판으로 들어간다 |
  | 배너 | `game_start/{GAME,START}` · `game_over/{GAME,OVER}` | 라운드 시작·종료 연출. 지금은 `drawResult()`가 글자로 그린다 |
  | 캐릭터 여분 | `player/bazzi/{jump,ready,wait,live,flash_long,flash_short}` | 준비 화면 · 무적 점멸 · 대기 동작 |
  | 맵 배리에이션 | `tile_2..10` · `object_2..6` · `block_2..9`(+`_pop`) | 타일·블록 변화와 여러 테마. `SPECS`는 `_1`만 등록한다 |
  | 기타 | `player/{player1,player2,solo_player}` | — |

  반대로 **`public/sounds/`는 디렉터리 자체가 없어** 항상 합성음으로 떨어진다.
  `audio.ts`는 파일이 있으면 쓰도록 이미 되어 있다 (7.5, README «음원 넣기»)
