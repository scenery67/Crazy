# 크레이지 아케이드 클론 — 설계 문서

> 상태: 초안 v0.1 / 최종 수정 2026-08-03

## 0. 한 줄 요약

격자 기반 4인 대전 액션. 물풍선을 놓아 십자 물줄기로 상대를 **물방울에 가두고**,
갇힌 상대를 밟아 제거한다. 1단계 목표는 **싱글 플레이어 + AI 봇**, 최종 목표는 온라인 대전.

---

## 1. 기술 선택

- **언어/런타임**: TypeScript (strict)
- **렌더**: Canvas 2D (도형/색상만, 이미지 에셋 없음). 나중에 WebGL·스프라이트로 교체 가능하도록 렌더러를 격리
- **빌드**: Vite
- **테스트**: Vitest (시뮬레이션 코어는 DOM 없이 순수 테스트)
- **출시 경로**: itch.io(웹) → Tauri 래핑(Steam) → Capacitor(모바일)

### 선택 근거

최종 목표가 온라인 대전이다. 봄버맨류 온라인의 최대 난관은 서버 권위(authoritative) 동기화인데,
**클라이언트와 서버가 같은 시뮬레이션 코드를 실행**하면 이 문제가 대부분 사라진다.
TS는 브라우저와 Node에서 동일 코드가 돌아가므로 이 구조가 공짜다.

> 네이티브 모바일/콘솔이 1순위가 되면 Godot 4로 이주한다.
> 아래 2~5장(규칙·상수·상태 모델)은 스택과 무관하므로 그대로 재사용된다.

---

## 2. 핵심 설계 원칙

### 2.1 결정론적 시뮬레이션 (가장 중요)

```
step(state: GameState, inputs: InputFrame[]) -> GameState
```

- **고정 타임스텝 60Hz**. 렌더 프레임과 시뮬레이션 틱을 분리 (accumulator 패턴)
- **부동소수점 금지**. 모든 좌표·속도는 정수. `1 타일 = 1000 units`
- 난수는 시드 기반 PRNG(xorshift32)를 상태에 담아 전달. `Math.random()` 사용 금지
- `step`은 DOM, 타이머, I/O에 접근하지 않는다

> **결정론적이지만 순수 함수는 아니다.** `step`은 성능을 위해 상태를 제자리에서 변경한다.
> 롤백·리플레이에 필요한 스냅샷은 `cloneState()`로 명시적으로 뜬다.
> 매 틱 상태를 통째로 복사하는 것보다 이쪽이 넷코드에 유리하다 —
> 롤백은 어차피 "특정 시점의 스냅샷"만 필요하지 모든 틱의 사본이 필요하지 않다.

이걸 처음부터 지키면 아래가 전부 공짜로 따라온다:

| 얻는 것 | 방법 |
|---|---|
| 리플레이 | 시드 + 입력 로그만 저장 (수 KB) |
| 온라인 넷코드 | 서버가 같은 `step`을 권위 실행 / 클라 예측·롤백 |
| 버그 재현 | 입력 로그를 테스트 픽스처로 |
| AI 학습·평가 | 렌더 없이 초당 수만 틱 시뮬레이션 |

### 2.2 입력 인터페이스 통일

사람이든 AI 봇이든 네트워크 원격 플레이어든, **똑같은 `InputFrame`을 뱉는다.**

```ts
type InputFrame = {
  playerId: PlayerId;
  tick: number;
  move: Dir | null;      // Up | Down | Left | Right
  placeBubble: boolean;
};
```

봇이 `GameState -> InputFrame` 함수라면, 나중에 서버에 그대로 올릴 수 있다.
봇에게 렌더 정보나 특권을 절대 주지 않는다.

### 2.3 레이어 분리

```
[입력 소스]        [시뮬레이션]         [출력]
키보드 어댑터  ─┐                    ┌─ Canvas 렌더러
AI 봇         ─┼─→ step() → State ──┼─ 사운드
네트워크(나중) ─┘                    └─ 디버그 오버레이
```

렌더러는 State를 **읽기만** 한다. 보간(interpolation)은 렌더 레이어에서만 수행.

---

## 3. 게임 규칙

### 3.1 맵

- **15 x 13 타일** (가로 x 세로)
- 타일 종류:
  - `Empty` — 통과 가능
  - `Hard` — 파괴 불가. 맵 테두리 전체 + 내부의 `x % 2 === 0 && y % 2 === 0` 위치에 격자 배치
    (테두리를 포함하므로 짝수/짝수다. 스폰이 되는 홀수/홀수 좌표는 항상 열려 있다)
  - `Soft` — 물줄기로 파괴 가능. 파괴 시 확률적으로 아이템 드랍
- 스폰: 네 모서리. 각 스폰의 인접 3칸(L자)은 `Empty` 보장 — 시작하자마자 갇히는 상황 방지

### 3.2 물풍선 (Bubble)

- 플레이어당 동시 설치 개수 = `bubbleCapacity` (기본 1)
- 설치 후 **3초(180틱)** 뒤 폭발
- 물풍선은 다른 플레이어에게 **벽**이다. 단 **방금 자기가 놓은 물풍선 위에서는 빠져나올 수 있다** (봄버맨 표준)
  - 구현: **"지금 그 타일에 겹쳐 있는 플레이어에게만 뚫려 있다"** — 이 한 줄이 전부다.
    별도 상태를 저장하지 않는다. 자연히 아래가 전부 성립한다:
    자기 물풍선에서 걸어 나올 수 있고 / 벗어나면 다시 못 들어가고 /
    설치 순간 같은 타일에 있던 다른 플레이어도 갇히지 않는다

### 3.3 물줄기 (Water)

- 폭발 시 **십자(+) 4방향**으로 `power` 타일만큼 뻗음 (기본 1)
- 진행 규칙:
  - `Hard` 만나면 **그 앞에서 정지**
  - `Soft` 만나면 **그 블록을 파괴하고 정지** (뚫고 지나가지 않음)
  - 다른 물풍선을 만나면 **즉시 연쇄 폭발** (같은 틱에 처리 — BFS 큐로)
  - 아이템을 만나면 아이템 소멸
- 물줄기 지속 시간: **0.5초(30틱)**. 이 동안 닿는 플레이어는 트랩

### 3.4 물방울 트랩 — 이 게임의 정체성

봄버맨과 갈리는 지점. 물줄기에 맞아도 **즉사가 아니다.**

```
[정상] --물줄기 피격--> [트랩(물방울에 갇힘)] --5초 경과--> [사망]
                              │
                              ├── 방향키 연타로 탈출 게이지 만땅 --> [정상] (무적 1초)
                              ├── 물줄기가 물방울 명중 (소유자 무관) --> [정상] (즉시 구출)
                              ├── 아군이 물방울에 접촉 --> [정상] (즉시 구출)
                              └── 적팀이 물방울에 접촉 --> [사망] (즉시 터뜨림)
```

> **물방울은 닿으면 터진다. 누가 터뜨렸는지가 결과를 가른다.**
> 적이 닿으면 죽고, 아군이 닿으면 산다. 둘 다 있으면 적이 이긴다
> (아군이 먼저 닿았다면 이미 풀려났을 것이므로).

> **구출은 소유자 무관이다.** 적이 쏜 물줄기여도 물방울에 닿으면 풀려난다(원작 룰).
> "적을 가둔 뒤 그 근처에서 물풍선을 함부로 못 터뜨린다"는 긴장감이 여기서 나온다.

#### 아군 접촉 구출을 넣은 이유 (플레이 테스트 결과)

처음에는 물줄기로만 구출할 수 있게 하고 아군 접촉은 "그냥 통과"로 두었다.
2v2를 실제로 해보니 **구출이 사실상 불가능했다.** 창이 이만큼밖에 없다:

```
트랩 지속 300틱(5초) − 물풍선 신관 180틱(3초) = 120틱 = 2초
```

갇힌 지 2초 안에 물풍선을 놓아야 하고, 파워 1이면 바로 옆 칸이어야 한다.
이미 옆에 서 있던 경우가 아니면 안 된다. 게다가 **달려가서 닿는 것**이
가장 직관적인 구조 동작인데 그게 무반응이었다.

규칙 표는 "동작한다"고 되어 있었지만 실제로는 아군에게 구조 수단이 없었다.
유닛 테스트로는 잡히지 않는 종류의 결함이다 — 규칙은 각각 정확히 동작했다.

- 트랩 지속: **5초(300틱)**. 시간 초과 시 사망
- **갇힌 직후 `RESCUE_GRACE`(=물줄기 수명) 동안은 구출 판정을 하지 않는다.**
  물줄기는 0.5초간 남아 있으므로, 이 유예가 없으면 나를 가둔 바로 그 물줄기가
  다음 틱에 나를 곧바로 풀어준다. 물줄기 수명만큼 기다리면 그 물줄기는 반드시 사라져 있다
- 자력 탈출: 좌우 방향키를 번갈아 누를 때마다 게이지 +1, `ESCAPE_THRESHOLD`(기본 12) 도달 시 탈출
- 트랩 상태에서는 이동 불가, 물풍선 설치 불가
- 탈출/구출 직후 **1초 무적**
- 트랩된 플레이어의 타일은 다른 플레이어의 통행을 막지 않는다 (밟으러 들어가야 하므로)

### 3.5 아이템

`Soft` 블록 파괴 시 30% 확률로 드랍. 종류별 가중치:

| 아이템 | 효과 | 가중치 |
|---|---|---|
| 물풍선 | `bubbleCapacity += 1` (최대 8) | 25 |
| 물물약 | `power += 1` (최대 8) | 25 |
| 롤러 | `speedLevel += 1` (최대 5) | 20 |
| 바늘 | 트랩 시 1회 즉시 탈출 (스택 가능) | 10 |
| 물약(초강화) | `power`를 일시적으로 최대치로 (15초) | 5 |
| 해골 | 랜덤 디버프 15초 (속도 저하 / 물풍선 감소 / 강제 설치) | 5 |
| 방패 | 5초 무적 | 10 |

**일시 효과는 원래 능력치를 건드리지 않는다.** 물약·해골은 남은 틱만 `Player`에 들고
있다가 `stats.ts`가 계산 시점에 덮어쓴다(`effectivePower`, `effectiveCapacity`,
`effectiveSpeed`). 효과가 끝날 때 "원래 값으로 되돌리는" 코드가 없으므로,
효과가 겹치거나 중간에 아이템을 더 먹어도 값이 어긋나지 않는다.
물풍선의 위력은 **설치 시점에 고정**되므로 물약이 끊겨도 이미 놓은 것은 그대로 터진다.

부순 블록에서 나온 아이템은 **그 폭발에는 지워지지 않는다.** 물줄기가 걷힌 뒤
드러나는 것이 원작의 순서다. 이후의 다른 폭발에는 지워진다.

### 3.6 팀 (확정: 지원함)

모든 플레이어는 `teamId`를 가진다. **개인전은 "전원이 서로 다른 팀"인 특수 케이스**로 처리한다.
별도 분기를 만들지 않는다.

| 상호작용 | 아군 | 적 | 자기 자신 |
|---|---|---|---|
| 물줄기 피격 → 트랩 | **된다** (프렌들리 파이어 ON) | 된다 | **된다** (자멸 가능) |
| 물방울에 물줄기 명중 → 구출 | 된다 | **된다** (소유자 무관) | 된다 |
| 물방울에 접촉 | **구출** | **제거** | — |

- 프렌들리 파이어를 켜두는 이유: 팀전에서 아군이 아무 위험이 아니면 좁은 통로 플레이가 사라진다
- 갇힌 사람은 남을 구할 수도, 밟을 수도 없다
- 팀 판정이 필요한 곳은 접촉 처리 한 군데뿐이라, 실제 팀 로직은 `systems/trap.ts`에만 있다

### 3.7 승리 조건

- **마지막 생존 팀**이 승리 (`alive`인 플레이어들의 `teamId`가 1종류만 남았을 때)
- 전원 동시 사망 시 무승부
- **서든데스**: 3분 경과 시 바깥쪽부터 나선형으로 `Hard` 블록이 떨어져 맵을 좁힘. 맞으면 즉사

---

## 4. 수치 상수 (튜닝 대상)

```ts
export const TICK_RATE   = 60;
export const TILE        = 1000;  // sub-units per tile

// 이동 속도 (units per tick) — TILE=1000, 60Hz 기준
export const SPEED_TABLE = [40, 46, 52, 58, 64, 70]; // 2.4 ~ 4.2 타일/초

// 히트박스: 타일보다 작게 잡아야 조작감이 산다
export const PLAYER_HITBOX = 600;  // 0.6 타일
export const CORNER_ASSIST =  60;  // 수직 보정 속도 (units/tick)

export const BUBBLE_FUSE     = 180; // 3.0초
export const WATER_DURATION  =  30; // 0.5초
export const TRAP_DURATION   = 300; // 5.0초
export const INVULN_DURATION =  60; // 1.0초
export const ESCAPE_THRESHOLD = 12; // 방향키 연타 횟수
export const SUDDEN_DEATH_AT = 60 * 180; // 3분
```

### 조작감의 핵심: 수직축 정렬

세로로 이동하면 히트박스(0.6타일)가 **두 행에 걸친다.** 그 상태에서 가로를 누르면
벽 모서리에 스쳐 막힌다. 격자 게임이 답답하게 느껴지는 원인이 대부분 이것이다.

규칙은 하나다:

> 축 이동 중에는 수직 좌표를 **"갈 수 있는 레인"의 중심**으로 끌어당긴다.
> - 두 레인에 걸쳤고 진행 방향이 **한쪽만** 열려 있으면 → 그 레인으로 (코너 어시스트)
> - 그 외에는 → 가장 가까운 레인으로 (자동 정렬)

처음엔 코너 어시스트와 자동 정렬을 별개 함수로 나눴는데,
그러면 코너를 돈 뒤 레인 중심에 못 미친 채(중심에서 180 units 벗어난 위치) 멈춘다.
막히지 않게 된 순간 보정이 꺼지기 때문이다. 두 규칙을 하나로 합쳐야 정확히 안착한다.

`CORNER_ASSIST`는 이 보정 속도다. 걷는 속도(40)보다 약간 빠르게 잡아야
"코너를 돌아 걸어간다"고 느껴진다. 너무 크면 순간이동처럼 보인다.

**단, 빠른 보정은 코너를 돌 때만 쓴다.** 그냥 레인에 안착하는 경우까지 빠르게 하면
보정이 이동을 앞지른다. 실제로 그렇게 만들었더니, 방향을 매 틱 번갈아 누르는 입력에서
전진 40이 보정 60에 되밀려 **플레이어가 스폰 타일에 못 박힌 채 한 발짝도 못 나갔다.**
통합 테스트가 아니었으면 못 잡았을 버그다.

### 이 게임은 레인 고정이다

보정 속도를 걷는 속도와 같게 맞춰도, 방향을 매 틱 번갈아 누르면 전진과 보정이
1:1로 상쇄되어 제자리에 머문다. **이건 버그가 아니라 장르의 정상 동작이다.**
봄버맨/크레이지아케이드에서 플레이어는 항상 어느 한 레인 위에 있고,
대각선 위치에 존재할 수 없다. 오른쪽으로 가다 아래를 누르면 먼저 열 중심으로 끌려간다.

따라서 **자동화된 입력(테스트·AI 봇)은 방향을 일정 시간 유지해야 한다.**
매 틱 랜덤 방향을 뽑는 입력기는 이 게임에서 의미 있는 부하가 되지 못한다.

---

## 5. 상태 모델

```ts
type GameState = {
  tick: number;
  rng: number;                  // xorshift32 시드
  phase: 'playing' | 'suddenDeath' | 'over';
  map: Uint8Array;              // 15*13, TileType
  players: Player[];
  bubbles: Bubble[];
  waters: Water[];
  items: Item[];
};

type Player = {
  id: PlayerId;
  teamId: TeamId;               // 개인전 = 전원 서로 다른 값
  alive: boolean;
  x: number; y: number;         // sub-unit 좌표
  facing: Dir;
  status: 'normal' | 'trapped' | 'invulnerable';
  statusTicks: number;          // 남은 틱
  escapeGauge: number;
  bubbleCapacity: number;
  bubblesPlaced: number;
  power: number;
  speedLevel: number;
  needles: number;
};

type Bubble  = { id, ownerId, tx, ty, fuse, power };
type Water   = { tx, ty, ticksLeft, ownerId, kind: 'center' | 'arm' | 'tip' };
type Item    = { tx, ty, kind };
```

- `map`은 `Uint8Array` — 상태 스냅샷/전송이 값싸다
- 엔티티는 배열. id는 단조 증가 정수. 삭제는 swap-remove

---

## 6. 디렉터리 구조

```
crazy/
├─ DESIGN.md
├─ package.json            (npm workspaces)
├─ packages/
│  ├─ core/                # 순수 TS. DOM 의존 0. 시뮬레이션 전부
│  │  ├─ src/
│  │  │  ├─ types.ts
│  │  │  ├─ constants.ts
│  │  │  ├─ rng.ts
│  │  │  ├─ map.ts         # 맵 생성 / 타일 질의
│  │  │  ├─ sim.ts         # step() 진입점
│  │  │  ├─ systems/
│  │  │  │  ├─ movement.ts    # 이동 + 충돌 + 코너 어시스트
│  │  │  │  ├─ bubble.ts      # 설치 / 신관
│  │  │  │  ├─ explosion.ts   # 물줄기 전파 + 연쇄
│  │  │  │  ├─ trap.ts        # 트랩 / 탈출 / 구출 / 밟기
│  │  │  │  ├─ item.ts        # 드랍 / 획득
│  │  │  │  └─ victory.ts     # 서든데스 / 승패
│  │  │  └─ ai/
│  │  │     ├─ bot.ts         # GameState -> InputFrame
│  │  │     ├─ danger.ts      # 위험 맵 (폭발 예정 타일 + 남은 틱)
│  │  │     └─ pathfind.ts    # BFS
│  │  └─ test/
│  └─ client/              # 브라우저
│     └─ src/
│        ├─ main.ts        # 게임 루프 (accumulator)
│        ├─ render.ts      # Canvas 2D
│        ├─ input.ts       # 키보드 -> InputFrame
│        └─ debug.ts       # 위험맵/히트박스 오버레이
└─ (나중) packages/server/  # Node. core 재사용
```

**규칙: `core`는 `client`를 절대 import하지 않는다.** 이 한 줄이 온라인 확장의 전제조건.

---

## 7. AI 봇 설계

난이도별로 같은 프레임워크, 파라미터만 다르게. (`core/src/ai/`)

1. **위험 맵** (`danger.ts`) — 각 타일이 **몇 틱 뒤에** 물줄기에 덮이는가.
   "지금 위험한가"가 아니라 "언제 위험해지는가"여야 놓고 빠져나올 수 있는지 판단된다.
   연쇄 폭발까지 고정점 반복으로 전개한다 — 남의 물풍선이 내 물풍선을 앞당겨 터뜨린다.
   **정지 규칙이 `explosion.ts`와 정확히 같아야 한다.** 어긋나면 봇이 헛다리를 짚는다
2. **시간 인지 BFS** (`pathfind.ts`) — 거리만으로는 부족하다.
   "도착했을 때 그 타일이 안전한가"를 같이 봐야 물줄기 속으로 걸어 들어가지 않는다.
   그래서 통행 판정이 도착 예정 틱을 함께 받는다
3. **행동 우선순위** (`bot.ts`)
   1. 갇혔으면 **탈출 연타** — 게이지는 방향을 *바꿀 때만* 오르므로 매 틱 번갈아 누른다
   2. 위험하면 **도망** — 다른 건 생각하지 않는다
   3. 놓을 만하고 **탈출 경로가 있으면** 설치
   4. **갇힌 아군 구조** → 아이템 → 부술 블록 옆 → 적 쪽 순으로 이동.
      구조가 아이템보다 앞인 이유: 아군은 5초 안에 죽지만 아이템은 도망가지 않는다
4. **탈출 경로 확인** — 가상의 물풍선을 포함한 위험 맵을 만들고, 폭발 전에 닿을 수 있는
   "폭발 예정이 전혀 없는" 타일이 있는지 본다. **이 검사 하나가 봇 품질의 대부분이다**
5. **계획은 타일이 바뀔 때만 새로 세운다.** 매 틱 새 방향을 뽑으면 레인 정렬이
   전진을 상쇄해 제자리에서 굳는다(2.1의 레인 고정 참조).
   못 움직인 채 12틱이 지나면 교착으로 보고 다시 계획한다
6. **난이도** — `aggression`(설치 적극성), `recklessness`(탈출 확인을 건너뛸 확률),
   `mashInterval`(연타 속도). 실수를 넣는 손잡이는 `recklessness` 하나뿐이다

---

## 8. 구현 로드맵

| 단계 | 산출물 | 검증 방법 |
|---|---|---|
| ~~**M1** 골격~~ | ✅ 워크스페이스, 타입, 상수, 맵 생성, 빈 `step`, Canvas에 맵 렌더 | 맵이 화면에 보인다 |
| ~~**M2** 이동~~ | ✅ 이동 + 충돌 + 수직축 정렬 + 키보드 2인 입력 | 답답하지 않게 돌아다닐 수 있다 |
| ~~**M3** 폭발~~ | ✅ 물풍선 설치·신관·십자 전파·연쇄·블록 파괴 | 연쇄 폭발 유닛 테스트 |
| ~~**M4** 트랩~~ | ✅ 피격→트랩→탈출/구출/밟기/사망, 승리 조건 | 1인 자멸 시나리오 테스트 |
| ~~**M5** 아이템~~ | ✅ 드랍 테이블, 획득, 일시 효과, 해골 디버프 | 밸런스 확인 |
| ~~**M6** 봇~~ | ✅ 위험맵, 시간 인지 BFS, 자살 방지 설치 판정, 난이도 3종 | 봇 vs 봇 헤드리스 대전 |
| **M7** 다듬기 | ~~서든데스~~ ✅ / 파티클, 사운드, UI, 리플레이 | 실제로 재미있는가 |
| **M8** 온라인 | `packages/server`, 서버 권위 + 클라 예측 | — |

M6의 **봇 vs 봇 헤드리스 대전**이 중요하다. 렌더 없이 시뮬만 돌려
"평균 게임 길이", "자멸 비율", "아이템 획득 분포"를 뽑으면 밸런스를 정량적으로 잡을 수 있다.

### 헤드리스 대전에서 나온 숫자

10판 × 난이도 3종, 최대 300초:

| | 평균 시간 | 종료 | 놓은 물풍선 | 부순 블록 |
|---|---|---|---|---|
| easy | 155초 | 10/10 | 110 | 69 (전부) |
| normal | 178초 | 10/10 | 155 | 69 (전부) |
| hard | 188초 | 10/10 | 161 | 69 (전부) |

**서든데스를 넣기 전에는 hard 봇끼리 0/10 종료였다.** 봇이 블록을 다 부수고 나면
서로 잘 피해다녀서 2분이 지나도 결판이 나지 않는다. 서든데스는 연출이 아니라
경기를 끝내는 유일한 장치다 — 실력이 붙을수록 없으면 안 된다.

무승부 비율이 40% 정도로 높다(마지막 둘이 같은 틱에 죽음). 4인 개인전 + 서든데스의
자연스러운 결과라 그대로 두었지만, 팀전 위주로 갈 경우 다시 볼 값이다.

### M6 시작 전에 알아둔 것 (통합 테스트에서 관측됨)

방향을 20~60틱씩 유지하며 무작정 돌아다니는 입력기는 **8초 안에 전원 자멸한다.**
자기 물풍선에 갇히고, 탈출에 필요한 방향 전환 12회를 트랩 시간(5초) 안에 못 채우기 때문이다.
봇은 최소한 다음 둘을 해야 사람 수준의 생존이 나온다:

1. **설치 전 탈출 경로 확인** — 놓고 나서 살아나갈 길이 있을 때만 놓는다
2. **갇혔을 때 연타** — 트랩 상태에서는 방향을 매 틱 번갈아 출력한다 (게이지는 방향 *전환*에만 오른다)

또한 봇은 이동할 때 **방향을 유지해야 한다.** 매 틱 새 방향을 뽑으면
레인 정렬이 전진을 상쇄해서 그 자리에서 굳는다.

---

## 9. 미결정 사항

- [x] ~~팀전(2v2) 지원 여부~~ → **지원함.** `teamId`를 초기 모델에 포함 (3.6 참조)
- [ ] 캐릭터별 고유 능력 유무 (원작에는 있음)
- [ ] 맵을 하드코딩할지 절차적 생성할지 — 우선 시드 기반 생성 + 고정 레이아웃 몇 개
- [ ] 모바일 터치 조작 (가상 조이스틱) — **보류.** 지금은 키보드 전용이라
      배포 링크를 폰으로 여는 테스터는 플레이할 수 없다. 테스터 수를 늘려야 할 때 붙인다
